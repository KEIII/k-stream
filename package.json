{
  "name": "k-stream",
  "version": "0.0.1",
  "description": "Functional reactive stream library for TypeScript",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "jest",
    "coveralls": "jest --coverage && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js",
    "prettier": "prettier --write ./src/*.ts"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^25.2.1",
    "coveralls": "^3.0.11",
    "jest": "^25.3.0",
    "prettier": "^2.0.4",
    "rxjs": "^6.5.5",
    "ts-jest": "^25.3.1",
    "typescript": "^3.8.3"
  }
}
